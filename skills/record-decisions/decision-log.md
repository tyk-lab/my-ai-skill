# 会话关键信息历史

仅在用户明确要求记录当前会话要点、查阅或恢复会话信息时使用。回顾历史时按主题读取最新且未被取代的条目。

## 当前有效上下文索引

仅保留状态为“进行中”“等待用户”“受阻”或“暂停”的主题；每个主题 ID 只能有一行。记录新快照后更新此表；主题完成后移除其行，历史快照保持不变。

| 主题 ID | 主题 | 状态 | 最近更新 | 下一步 | 最新快照 |
|---------|------|------|----------|--------|----------|
| quatro-extruder-motor-test | Quatro ToolBoard 挤出电机循环测试 | 进行中 | 2026-08-03 | 在实际硬件上验证电机方向、速度、循环动作及 F_UNLOAD 故障急停 | 2026-08-03 — Quatro ToolBoard 挤出电机循环测试 |

## 历史快照

## 2026-08-03 — Quatro ToolBoard 挤出电机循环测试

- 主题 ID：`quatro-extruder-motor-test`
- 类型：方向确认
- 状态：进行中
- 目标与范围：在 `printer-test-quatroToolBoard.cfg` 的宏区域持续循环测试挤出电机；仅涉及该文件中的挤出机驱动参数、`F_UNLOAD` 事件和电机测试宏。
- 已对齐信息：动作序列、电流、速度及临时信号旁路需求由用户在对话中明确指定。
- 已确认决定：每轮进料 100 mm，再执行 3 次“退料 20 mm、进料 20 mm”；电机运动期间 `F_UNLOAD` 应保持触发，否则按故障立即关停。挤出机额定电流固定为 0.5 A，测试速度固定为 8 mm/s。使用 `RUN_MOTOR_TEST` 启动、`STOP_MOTOR_TEST` 停止；提供临时变量 `ignore_f_unload`，默认 `False`，仅当设为 `True` 时忽略全部 `F_UNLOAD` 检查。
- 条件与约束：非临时测试时保持 `ignore_f_unload=False`。
- 当前状态/已完成：测试参数与宏方向已确认，尚未进行硬件实测。
- 待处理：在实际硬件上验证电机方向、8 mm/s 速度、循环动作及 `F_UNLOAD` 故障急停。
- 待澄清：无
- 依据：用户在对话中明确指定动作序列、电流、速度及临时信号旁路需求。
- 取代关系：无
